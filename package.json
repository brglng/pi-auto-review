{
  "name": "@brglng/pi-auto-review",
  "version": "0.14.0-brglng.1",
  "description": "Fail-closed, model-backed approval broker for Pi: deterministically hard-denies dangerous operations, reviews dangerous boundaries with a reviewer model, and issues one-shot expiring grants that OS sandbox adapters must consume before retrying.",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi-extension",
    "pi",
    "security",
    "sandbox"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/brglng/pi-auto-review.git"
  },
  "homepage": "https://github.com/brglng/pi-auto-review",
  "bugs": {
    "url": "https://github.com/brglng/pi-auto-review/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "files": [
    "src",
    "CHANGELOG.md",
    "README.md",
    "LICENSE"
  ],
  "exports": {
    ".": "./src/index.ts",
    "./broker": "./src/broker/index.ts",
    "./sandbox": "./src/integrations/sandbox.ts"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --disable-warning=ExperimentalWarning --experimental-loader ./test/typescript-loader.mjs --test test/*.test.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "^0.84.1",
    "@earendil-works/pi-coding-agent": "^0.84.1",
    "@gotgenes/pi-permission-system": "^27.0.0"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.84.1",
    "@earendil-works/pi-coding-agent": "^0.84.1",
    "@gotgenes/pi-permission-system": "27.1.0",
    "@types/node": "^26.2.0",
    "typescript": "^6.0.3"
  }
}
